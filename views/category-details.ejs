<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= event.title %> | Robolution</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/event-details.css">
  <style>
    /* Dropdown styles */
    .dropdown {
      position: relative;
      display: inline-block;
    }
    
    .dropdown-btn {
      cursor: pointer;
    }
    
    .dropdown-content {
      display: none;
      position: absolute;
      background-color: #f9f9f9;
      min-width: 220px;
      box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
      z-index: 1;
      border-radius: 4px;
      padding: 8px 0;
    }
    
    .dropdown-content a {
      color: black;
      padding: 12px 16px;
      text-decoration: none;
      display: block;
      text-align: left;
      transition: background-color 0.2s ease;
    }
    
    .dropdown-content a:hover {
      background-color: #f1f1f1;
    }
    
    .dropdown:hover .dropdown-content {
      display: block;
    }
    
    .dropdown-empty {
      padding: 12px 16px;
      color: #666;
      font-style: italic;
    }
  </style>
</head>
<body>
<div class="content-wrapper">
  <main class="event-details-main">
    <div class="event-details-card">
      <h1 class="event-details-title"><%= event.title %></h1>
      <p class="event-details-desc"><%= event.description %></p>
      <% if (event.sections) { %>
        <% event.sections.forEach(function(section) { %>
          <h2 class="event-details-section-title"><%= section.title %></h2>
          <% if (section.type === 'list') { %>
            <ol class="event-details-list">
              <% section.items.forEach(function(item) { %>
                <li><%= item %></li>
              <% }); %>
            </ol>
          <% } else if (section.type === 'bullets') { %>
            <ul class="event-details-bullets">
              <% section.items.forEach(function(item) { %>
                <li><%= item %></li>
              <% }); %>
            </ul>
          <% } else if (section.type === 'text') { %>
            <p><%= section.content %></p>
          <% } %>
        <% }); %>
      <% } else if (event.mechanics && event.mechanics.length) { %>
        <h2 class="event-details-section-title">Mechanics</h2>
        <ul class="event-details-bullets">
          <% event.mechanics.forEach(function(item) { %>
            <li><%= item %></li>
          <% }); %>
        </ul>
      <% } %>
      <% if (event.generalConduct && event.generalConduct.length) { %>
        <h2 class="event-details-section-title">General Conduct</h2>
        <ul class="event-details-bullets">
          <% event.generalConduct.forEach(function(item) { %>
            <li><%= item %></li>
          <% }); %>
        </ul>
      <% } %>
      <% if (event.generalRules && event.generalRules.length) { %>
        <h2 class="event-details-section-title">General Rules</h2>
        <ul class="event-details-bullets">
          <% event.generalRules.forEach(function(item) { %>
            <li><%= item %></li>
          <% }); %>
        </ul>
      <% } %>
      <% if (event.participantsRequirement && event.participantsRequirement.length) { %>
        <h2 class="event-details-section-title">Participants Requirement</h2>
        <ul class="event-details-bullets">
          <% event.participantsRequirement.forEach(function(item) { %>
            <li><%= item %></li>
          <% }); %>
        </ul>
      <% } %>
      <% if (event.teamRequirement && event.teamRequirement.length) { %>
        <h2 class="event-details-section-title">Team Requirement</h2>
        <ul class="event-details-bullets">
          <% event.teamRequirement.forEach(function(item) { %>
            <li><%= item %></li>
          <% }); %>
        </ul>
      <% } %>
      <a href="/categories" class="back-btn">← Back to Categories</a>
    </div>
  </main>
</div>
</body>
</html>
